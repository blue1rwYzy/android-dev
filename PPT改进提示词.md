# 智能 TodoLife 项目展示 PPT —— 重构与丰富提示词（供 Claude Code 接续执行）

> 用途：把 `智能TodoLife项目展示.pptx`（16:9 / 13.33″×7.5″ / 15 页）从“字太小、视觉空洞”重构为符合行业规范、内容充实的产品案例 Deck。
> 执行方式：用 `python-pptx` 在原文件基础上**逐页重排版**（不要从零新建），生成新文件 `智能TodoLife项目展示_v2.pptx`，保留全部中文文案与叙事结构，只升级排版/字号/配色/图形化/信息密度。

---

## 0. 任务总目标

当前 PPT 的致命伤是**字号严重不达标**（正文大量为 5–10pt，标题仅 24pt，封面 32pt）且**版面空洞**（纯文字、无大数字焦点、无设备样机、图表无标注）。
你的任务：在保留现有 15 页叙事骨架（封面→背景→目标→能力图→信息架构→数据模型→AI规划→认知模型→优先级→艾森豪威尔→今日体验→多视图→统计→技术架构→总结）的前提下，逐页应用下方“设计规范”，让每页**既符合演示行业标准、又内容饱满不空洞**。

执行硬性验收：
1. 全文**没有任何文字 < 18pt**；正文统一 ≥24pt；说明性 caption ≥18pt。
2. 每页有**明确视觉焦点**（大标题 / 大数字 / 主视觉之一）。
3. 关键数据页出现 **≥60pt 的 Hero 大数字**。
4. 至少 6 页采用 **Bento 网格卡片**布局（非通栏纯文字段落）。
5. 配色遵循 60-30-10、单一强调色、正文对比度 ≥4.5:1。

---

## 1. 排版字号规范（pt）—— 必须严格套用

| 角色 | 字号 | 字重 |
|---|---|---|
| 封面主标题 | **48–56** | Bold |
| 内页标题（中文主标题行） | **40** | Bold |
| 英文栏目标签（如 PRODUCT CASE / 页眉小写英文） | **16–18**（字间距加宽 +2） | Medium，强调色 |
| 副标题 / 区块标题 | **28–30** | Semibold |
| 正文 | **24**（只读屏可降到 22，不得更低） | Regular |
| 卡片内二级文字 / 列表 | **20** | Regular |
| 说明文字 / caption / 标签 | **18**（图表数据标签可 16） | Regular |
| **Hero 大数字（KPI 焦点）** | **60–96** | Bold |
| KPI 标签 | **≥28** | Regular |
| 脚注 / 来源 | **14**（不低于此） | Regular |

规则：标题 ≈ 2× 正文；正文 ≈ 1.5× caption。**单页最多 3 种字号、最多 2 种字族**。行距：标题 1.0–1.15×，正文 1.3–1.5×。

### 字体（中文 + 拉丁/数字 配对）
- 中文主字体：**思源黑体 / Source Han Sans**（缺失则 `Microsoft YaHei 微软雅黑`）。标题用 Bold/Heavy，正文用 Regular，靠字重拉层次。**投影场景禁用宋体**（衬线发虚）。
- 拉丁文与数字：配 **Inter / Helvetica Neue / Segoe UI / Arial**。**Hero 大数字一律用拉丁无衬线（如 Inter/Helvetica），不要用中文字体渲染数字**。
- `python-pptx` 设置示例：`run.font.name='Source Han Sans SC'`；并对 `run.font.element` 设置东亚字体 `<a:ea typeface="思源黑体">`、拉丁 `<a:latin typeface="Inter">`。

---

## 2. 版面与留白

- **页边距**：左右 0.67″、上下 0.75″（可接受 0.5–0.75″）；所有页元素对齐到同一组边距，逐页一致。
- **网格**：12 列骨架（实操用 6 或 8 列）+ 列间距；关键元素压在三分线“能量点”上，每页 ≤2 个。
- **留白**：每页保留 **15–20% 空白**，留白是元素不是浪费。
- **密度**：一页一个核心观点；遵循 1-6-6（≤6 条、每条 ≤6 词）作为**上限**。装不下就拆页或删，**绝不靠缩小字号塞内容**。
- **对齐**：中文正文左对齐；每页单一焦点；最重要信息放左上（视线首落点）。
- **文图比**：整套 60–80% 文字 : 20–40% 视觉，视觉必须服务表达。

---

## 3. 配色系统

- **60-30-10**：60% 主中性色（背景）· 30% 次色（正文+辅助）· 10% **单一强调色**（仅用于关键数据/重点，占比绝不超过 ~10%）。先用灰度排版，最后加色。
- **对比度（WCAG）**：正文 ≥4.5:1（投影目标 7:1）；大字（≥18pt 或 ≥14pt 粗体）≥3:1；非文字元素（柱/图标/分隔线）≥3:1。用 WebAIM 校验。
- **强调色**：选高对比 橙 / 青 / 暗底黄；**优先蓝-橙而非红-绿**（色盲友好）；颜色只用于“信号”不用于装饰，配合形状/标签冗余。
- **图表分类色**：≤6 种（软上限 8，绝对上限 12），超了合并“其他”或换图型，**禁止循环复用颜色**。
- **明暗策略（推荐混合）**：封面 / 章节分隔页 / App 截图整屏展示页用**深色背景**（电影感、让 UI 跳出）；文字密集页（背景/流程/分析）用**浅色背景**减轻阅读疲劳。深色页正文用白/浅灰，强调用黄/青/橙（**禁霓虹色**），纯白软化为象牙白/浅灰防投影眩光。
- 复用本项目 App 主题色作为强调色，从 `app/src/main/java/com/example/android_dev/ui/theme/Color.kt` 取色，保证 Deck 与产品视觉一致。

---

## 4. 数据可视化与图形化（解决“空洞”的关键）

- **大数字 KPI**：每页 ≤3–4 个，最好 1 个焦点。三段式：①数字（≥60pt，主视觉）②短标签 ③上下文（对比目标/上周/预期）。补“所以呢”（如“完成率 68%，较上周 +12%”）。最关键数字放左上，多个 KPI 字号/位置一致；颜色只作信号。
- **图表（数据墨水比）**：去网格线/3D/冗余图例；**直接在柱/扇区上标数据**（删图例和坐标轴）；单位与主题写进标题；剩余网格线淡灰；**只把关键那一根柱/那一块扇区上色，其余置灰**；环形图优于饼图（中心放总数）；≤5 类；图表必须脱离悬浮也能读懂。
- **架构/流程图**：只画受众需要的；密图拆成多张联动小图；符号/术语/布局一致，每个节点都标注；**单色，仅用颜色高亮关键**；必要时加图例；避免“文字堆砌”，保持图形化。
- **对比表**：表头加底色+加粗+白字；隔行浅色底；数字右对齐、文字左对齐；边框极简浅灰。
- **App 截图/样机**：展示 3–5 张最佳界面（完整案例 4–10 张）；**统一一种设备/浏览器外框 + 同一代设备**，全套一致（混用是“糙 Deck”头号特征）；封面/Hero 用拟真外框，密集功能网格用极简“clay”外框；UI 先清理（真实数据、无空/调试态）；导出 2x / ≥1920×1080，裁到关键区域而非整体缩小；深色/渐变背景衬浅色 UI。
  - 本项目可截图来源界面：`TodayScreen`（今日）、`KanbanScreen`（看板）、`CalendarScreen`（日历）、`CountdownScreen`（倒计时）、`StatisticsScreen`/`InsightScreen`（统计）、`AiChatScreen`（AI 对话）、`ProfileScreen`（我的）。若无真机截图，用 python-pptx 画**手机外框占位符 + 内部模块草图**，并标注“界面示意”。

---

## 5. 现代审美 Do / Don't（2025–2026）

**DO**
- **Bento 网格**为主结构：一页 = 不对称模块卡（1 大特性块 + 若干小辅助块），让图表/数据/截图共存不乱（最高优先趋势）。
- **大胆极简**：高对比、自信的大字、强色块。
- **深色模式**用于封面/分隔/UI 展示；**字体即主视觉**（同一标题内粗细对比）。
- **玻璃拟态**克制使用：仅卡片/导航做毛玻璃模糊，配深色+柔和极光渐变提升质感。
- **图标**：统一、单色、精炼，与 KPI/特性块搭配。
- 结果先行（Minto 金字塔）；发现归为 3–5 个主题；每个方案回扣具体洞察；影响页要有数字。

**DON'T**
- 不做扁平企业蓝的“满屏项目符号”页（已过时）。
- 玻璃拟态不做底层；深色页不用霓虹色。
- **绝不为塞内容把字缩到 24pt 以下——拆页**。
- 不混用外框/背景风格；不放白底裸截图。
- 不超过 ~10% 强调色 / 3 种字号 / 6 种图表色。
- 不靠颜色单独传达信息；图表不靠悬浮标签；不用 3D/WebGL 噱头。

**优先级：Bento 网格（结构） > 大字+深色（基调） > 玻璃拟态（点缀）。**

---

## 6. 逐页改造清单（保留文案，升级呈现）

> 用项目真实技术细节充实，已从源码核对：优先级权重 = 重要度 0.36 / 紧迫 0.30 / 能力匹配 0.20 / 复杂度 0.14 / 习惯 0.08（见 `domain/SmartTodoModels.kt: PriorityWeights`）；6 引擎 = SmartTaskEngine / CognitiveLoadEngine / StatisticsEngine / AchievementEngine / TaskCompletionEngine + AI Planner（远程 RemoteAiTaskPlanner + 本地 LocalAiTaskPlanner 兜底）。

| 页 | 主题 | 改造要点 |
|---|---|---|
| 1 封面 | 智能 TodoLife | 深色背景；主标题 52pt Bold + 一句副标题 28pt；右侧放手机样机展示今日页；底部三标签（Jetpack Compose / 认知建模 / 智能排程）做胶囊 chip 18pt；右上 `PRODUCT CASE 01` 18pt 强调色 |
| 2 背景 | 为什么普通待办不够 | 4 张 Bento 卡（清单越积越多 / 优先级靠人判断 / 状态被忽略 / 缺“下一步”），每卡标题 28pt + 说明 20pt + 单色图标 |
| 3 产品目标 | 待办系统→集成系统 | 捕捉→理解→推荐→反馈 横向流程图，节点 28pt，箭头连接，底部一句价值收口 24pt |
| 4 能力图 | Smart Todo Engine | 中心圆 = Engine，6 条能力做卫星卡（今日页/AI对话/智能日程/看板/日历/统计/提醒）Bento 环绕 |
| 5 信息架构 | 页面结构 | 底部 5 Tab 导航条 + 上方树状层级图；用样机框示意各页 |
| 6 数据模型 | SmartTask | 左：5 类字段 Bento 卡（基础/执行/特征/状态/习惯）；右：一张 SmartTask 实例卡（category/estimatedMinutes/dueDate/priority/status/subtasks）做代码风格高亮卡，字段 20pt |
| 7 AI 规划 | 一句话→可执行计划 | 流程：用户输入→DueDateParser→SmartTaskEngine→AI Planner→子任务清单；强调“远程优先+本地兜底”双路；右侧示例 5 条子任务带日期/分钟 |
| 8 认知模型 | 量化负荷 | 4 维负荷（视觉/记忆/时间压力/决策疲劳）做仪表/进度条，低/平/高/过载色阶；一句自适应说明 24pt |
| 9 优先级逻辑 | 可解释推荐 | **Hero**：推荐分 **82.4**（96pt）+ 任务名标签；左侧 5 个权重做横向条形图，直接标 36%/30%/20%/14%/8% 与因子名；删图例 |
| 10 艾森豪威尔 | 四象限 | 2×2 矩阵，象限标题 28pt，每格 2 个示例任务卡（含分钟），右下“删除/批量”象限置灰弱化 |
| 11 今日体验 | 回答“现在做什么” | 中央手机样机（今日页）+ 三环活动数据（能量68/精力72/专注45）做 Hero 数字 60pt；右侧 3 卡：今日推荐/快速捕捉/状态调度 |
| 12 多视图 | 不同阶段不同视图 | 4 张样机并排（今日/看板/日历/倒计时）统一外框；每张下 caption 18pt 说明适用场景 |
| 13 统计 | 数据是叙事 | **Hero**：完成率 68%（72pt）；环形图（分类时间分布，≤6 色，中心放总时长）；习惯热力图缩略；右侧周报摘要卡 |
| 14 技术架构 | 分层 + AI 增强 | 6 层横向分层图（UI / 状态 / 引擎 / 数据 / AI / 系统服务），每层列真实类名 20pt；右侧标注“AI 失败→本地兜底”降级箭头 |
| 15 总结 | 设计价值 | 4 个编号价值卡 Bento（01–04），编号 60pt 弱色 + 标题 28pt；底部一句总收口 24pt |

---

## 7. 实现注意（python-pptx）

- 在**原 pptx 副本**上改：`prs = Presentation('智能TodoLife项目展示.pptx')`，逐页清理旧文本框后按规范重建，输出 `_v2`。
- 中文东亚字体需手动设 XML（`rPr` 下加 `a:ea`），否则只生效拉丁字体。
- Hero 数字、KPI 用独立文本框 + 字号 60–96pt；图表优先用 `add_chart`（柱/环形）并关闭图例、开启数据标签。
- 配色用 `RGBColor`，强调色集中定义为常量，全程复用同一支。
- 样机：若无截图素材，用圆角矩形画手机外框（`MSO_SHAPE.ROUNDED_RECTANGLE`）+ 内部模块占位，统一外框尺寸与圆角。
- 完成后自检：脚本扫描所有 run，断言无 `font.size < Pt(18)`，打印每页字号分布。

---

## 8. 引用的行业标准来源（已联网核实）

字号/可读性：BrightCarbon、Beautiful.ai、Superchart、Microsoft 365、Penn State、Guy Kawasaki 10/20/30、Presentation Guild 8H Rule、Duarte Glance Test。
版面/留白/网格：Deckary、Infodiagram、Slidor、SlideModel、SlideBazaar。
配色/对比：WebAIM、WCAG 1.4.3/1.4.6/1.4.11、Wix 60-30-10、Adobe Spectrum、Okabe-Ito、Edward Tufte。
数据可视化/样机：Holistics（数据墨水比）、Domo/Datylon（环形图）、Screenhance（样机外框）、OpenVC（产品页）。
2026 趋势：SlideEgg、Studio Meyer、IxDF、We Are Tenet（Bento / 大字 / 深色 / 玻璃拟态）。
